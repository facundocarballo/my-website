module.exports = {
  reactStrictMode: true,
  trailingSlash: true, // Esto es muy importante porque deja las extensiones de los archivos.
  // Gracias a esto podemos ver en la pagina oficial los markdown files originales.
  // Los archivos markdown no se muestran sin la extension '.html'.
  
}
